# MuttMind Runbook (Post-Clone)
1. Add env vars in `.env.local`.
2. Run SQL in `supabase/schema.sql`.
3. Bootstrap owner user/workspace in Supabase.
4. Configure Telegram webhook to `/api/telegram-webhook`.
5. Use `/login`, then `/dashboard`, then `/vault`.
