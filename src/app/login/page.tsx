'use client';
import { useState } from 'react'; import { supabaseBrowser } from '@/lib/client-auth';
export default function LoginPage(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [message,setMessage]=useState(''); async function signUp(){const {error}=await supabaseBrowser.auth.signUp({email,password});setMessage(error?error.message:'Signup successful.');} async function signIn(){const {error}=await supabaseBrowser.auth.signInWithPassword({email,password});setMessage(error?error.message:'Signed in. Go to /dashboard');} return <main><h1>Login</h1><div style={{display:'grid',gap:10,maxWidth:420}}><input placeholder='Email' value={email} onChange={(e)=>setEmail(e.target.value)}/><input type='password' placeholder='Password' value={password} onChange={(e)=>setPassword(e.target.value)}/><button onClick={signIn}>Sign in</button><button onClick={signUp}>Sign up</button><p>{message}</p></div></main>}
